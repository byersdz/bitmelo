var merge = require( 'webpack-merge' );
var common = require( './webpack.config.lib.common' );
var path = require( 'path' );

module.exports = merge( common, {
  mode: 'production',
  output: {
    path: path.resolve(__dirname, '../lib'),
    filename: 'minnow.min.js',
    library: 'minnow',
    libraryTarget: 'umd'
  }
});
